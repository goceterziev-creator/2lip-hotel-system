// ⚙️ script.js
class BookingSystem {
    constructor() {
        this.availableRooms = [];
        this.bookings = [];
    }
    
    checkAvailability(checkIn, checkOut) {
        // Логика за проверка на наличност
        return true;
    }
    
    makeReservation(room, guest) {
        // Логика за резервация
        console.log(`Резервация за ${guest.name}`);
    }
}